import React from "react";
import ReactDOM from "react-dom/client";
import RelicVault from "./RelicVault.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RelicVault />
  </React.StrictMode>
);
